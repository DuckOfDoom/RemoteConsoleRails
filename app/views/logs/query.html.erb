<%= grid(@logs_grid, show_filters: :always) do |g|

  g.column name: 'ID', attribute:'id' do |task|
    task.id
  end

  g.column name: 'Device ID', attribute:'device_id' do |task|
    task.device_id
  end

  g.column name: 'Log Type', attribute:'log_type', custom_filter: :auto do |task|
    task.log_type
  end

  g.column name: 'Log', attribute:'log' do |task|
    task.log
  end

  #g.column do |task|
  #  task.archived? ? 'Yes' : 'No'
  #end

  #g.column do |task|
  #  link_to('Edit', edit_task_path(task))
  #end
end -%>
